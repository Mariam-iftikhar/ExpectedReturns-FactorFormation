# Easy Task: Package Installation

library(remotes)
install_github("JustinMShea/ExpectedReturns")
install_github("braverock/FactorAnalytics")
install_github("braverock/FactorAnalytics", force = TRUE)


# Conclusion:

# •	ExpectedReturns and FactorAnalytics installed successfully.
# •	Some dependencies had permission issues, but that’s fixable with a restart or elevated privileges.
# •	No actual errors stopped the process — only warnings.



